# Workflows Sprint — execution state

**Goal:** Build the "Workflows" feature (reframe Agents → Workflows: a codified, configurable preset gallery, Mintlify-style) to production-ready, then keep improving it autonomously until Ricardo returns.

**Design source of truth:** `/Users/canales/Documents/obsidian/Breadbox/planned-features/workflows-preset-gallery-proposal.md` (settled through 3 rounds of decisions — read it for full rationale).

**Branch model:** Integration branch = `workflows/preset-gallery-sprint` (off main `c285101b`). Each unit of work is a topic branch `workflows/NN-slug` → PR **with base = `workflows/preset-gallery-sprint`** → validate → merge into the sprint branch (Ricardo gave explicit merge authority *within this branch*; do NOT merge to main). Ricardo reviews sprint→main later.
**CI:** `workflows/**` is in `.github/workflows/ci.yml` pull_request.branches, so PRs into the sprint branch get the full matrix (default / headless / lite). Wait for green before merging.
**Per iteration:** make progress on the next unit → validate (build/vet/test + UI screenshot evidence via img402.dev) → open PR with evidence → wait CI green → merge → update THIS file → `PushNotification` with iteration summary + PR link.

**⚠️ Integration test DB:** the shared `breadbox_test` has cross-worktree out-of-order migration conflicts (parallel worktrees apply their own migrations). Use the dedicated **`breadbox_test_wf`** DB instead: `DATABASE_URL='postgres://breadbox:breadbox@localhost:5432/breadbox_test_wf?sslmode=disable'` (created via the OS-superuser `canales`; the `breadbox` role lacks CREATEDB). CI is unaffected (fresh DB per run). Don't apply this sprint's breaking migrations to the shared dev `breadbox` DB (would break other running servers).

## Settled decisions (compact)
- "Workflows" **fully replaces** "Agents" across DB tables (`agent_definitions`→`workflows`, `agent_runs`→`workflow_runs`), API (`/api/v1/workflows/*`), UI (nav "Workflows"), MCP (`workflow_*`). Internal runtime stays "agent" (`actor_type='agent'`). Pre-release → breaking changes OK.
- Presets = **code-defined registry** (sibling of `prompt_blocks.go`, `//go:build !lite`), composes base prompts from `prompts/agents/*.md`. Enabling **instantiates** a `workflows` row (new `source_template` col). NOT seeded rows.
- **Auto-apply is default.** Safety = source-aware lock + flag + per-run undo (agents enrich-only, can't create/move money).
- `category_override` boolean → **enum** `{none, agent, user}` (keep the name). Precedence **user > agent > rule**: rules write only `'none'`; agents write where `<>'user'` and stamp `'agent'`; users override all and stamp `'user'`.
- **Flag** = `transactions.flagged_at` field (no reason col); reason → **comment annotation**.
- **`transactions.metadata` JSONB** enrichment store; agents write freely via **scoped ops** (set-one / remove-one / replace-all / clear; metadata-only, no other-field backdoor).
- Configure UX = **right-side drawer** (daisy `drawer drawer-end`, Mintlify-style), per-preset specialized options.
- **Iteration 1 presets (3):** ⭐ Routine Reviewer (post-sync, auto-apply+flag), Weekly Money Digest (cron, report-only), Subscription Auditor (cron, additive tags+report).
- Governance in iteration 1: household cost ceiling, consent ack, admin-only enable.
- Deferred (post–iteration 1): the other 13 presets, outbound notification sink (→ Sync Watchdog + Alerts category), custom-workflow builder.

## PR checklist (iteration 1)
- [x] **PR 1 — `transactions.metadata` JSONB field (standalone).** ✅ MERGED #1615 (squash 68180491). Migration + scoped MCP/REST ops (set/remove/replace/clear, metadata-only) + admin tx-detail key/value section + integration tests + openapi/api-endpoints. Per-key provenance via annotations deferred to the flag/comment PR.
- [x] **PR 2a — `category_override` boolean→enum (plumbing).** ✅ MERGED #1616 (squash 65fa31d2). Breaking type migration + backfill (TRUE→'user'/FALSE→'none'); `none`/`agent`/`user` CHECK; all rule-skip guards → `='none'`; response field + CLI client now string enum; openapi + docs + CLAUDE.md; `service.CategoryOverride{None,Agent,User}` consts; ~14 test files migrated + new `TestCategoryOverrideEnum`. Behavior-preserving (non-agent writes stamp 'user').
- [x] **PR 2b — agent precedence behavior.** ✅ MERGED #1618 (squash 19c36d7f). New `SetTransactionCategoryOverrideAgent` guarded query (`<>'user'`→`'agent'`); `update_transactions` + `SetTransactionCategory` branch on `actor.Type=='agent'`; user-locked agent writes report `Status:"skipped"` (+ succeeded/skipped/failed counts in handleUpdateTransactions); MCP descriptions document precedence+skip; `TestCategoryOverridePrecedence` proves the full ladder incl. tag-applies-on-skip. **PR2 (a+b) complete — category_override precedence done.**
- [x] **PR 3 — `transactions.flagged_at` + flag action.** ✅ MERGED #1620 (squash 38a243d3). Additive field + partial index; FlagTransaction/UnflagTransaction (flagged_at + reason→comment annotation); flag_transaction/unflag_transaction MCP tools; flagged filter on query_transactions/count + REST ?flagged=; POST/DELETE /transactions/{id}/flag; FlaggedAt on responses; admin Flagged filter+chip + row flag indicator; openapi/api-endpoints; TestFlagTransaction. **Follow-ups:** live UI screenshot (deferred — shared-DB conflict), mobile row indicator + detail-page flag indicator.
- **PR 4 — split into 4a/4b/4c:**
  - [x] **PR 4a — preset registry (additive).** ✅ MERGED #1621 (squash 67534f62). `agent_definitions.source_template` col; `workflow_presets.go` registry w/ 3 starter presets composing from `prompts/agents/` blocks; `EnableWorkflowFromPreset` (one-per-preset, ErrConflict) + `ListWorkflowPresets`; `GET /workflow-presets` + `POST /workflow-presets/{slug}/enable`; ErrConflict→409; unit+integration tests.
  - [ ] **PR 4b — agents→workflows API/UI/MCP rename (non-breaking-DB).** `/api/v1/agents/*`→`/workflows/*` (keep agents as alias or hard-rename), nav "Agents"→"Workflows", MCP `agent_*`→`workflow_*` tools, openapi+api-endpoints+external docs. Tables stay `agent_definitions` internally for now.
  - [ ] **PR 4c — DB-table rename `agent_definitions`→`workflows`, `agent_runs`→`workflow_runs`.** BREAKING migration (RENAME TABLE) — coordinate (do when other worktrees idle / shared-DB clear). sqlc regen `Workflow`/`WorkflowRun` + all refs. Atomic to compile.
- **PR 5 — split into 5a/5b/5c:**
  - [x] **PR 5a — gallery page (`/workflows`).** ✅ MERGED #1624 (squash 53325309). Admin page rendering the preset registry grouped by category (SettingsSection/SettingsRow), per-preset Enable button → `POST /-/workflow-presets/{slug}/enable`, run toggle (reuses agent enable/disable), trigger labels, `GetAgentSubsystemStatus` ready-banner, "Workflows" nav (lucide workflow) + pageSectionLabel, `workflows_gallery.js`. Real login→/workflows screenshot (Playwright headless + system Chrome against breadbox_test_wf; img402). All daisy-first, no new bb-*.
  - [x] **PR 5b — configure drawer.** ✅ MERGED #1626 (squash c1d9d483). Mintlify-style right slide-over (`fixed inset-y-0 right-0` panel + dimming backdrop, one per non-enabled preset): "Set up" replaces "Enable" → drawer with **When it runs** (schedule `<select>` Daily/Weekly/Monthly for scheduled presets, read-only "After each successful sync" for post-sync), **Additional instructions** textarea (appended under `## Additional instructions`, cap 4000 chars), **Start running now** checkbox, Cancel/Enable footer. `EnableWorkflowFromPresetParams{Enabled,ScheduleCron,AdditionalInstructions}` (schedule override ignored for post-sync); handler maps ErrConflict→already_enabled; `submitDrawer()` serializes form (explicit unchecked checkbox)+POSTs+reloads. +2 integration tests (`WithConfig`, `PostSyncIgnoresSchedule`). 2 screenshots (post-sync + scheduled drawer, img402). **Deferred to PR6:** projected-cost line in drawer.
  - [x] **PR 5c — runs tab.** ✅ MERGED #1628 (squash 2e9c7ca1). New `GET /workflows/runs` (`WorkflowRunsPageHandler`) reuses `ListAllAgentRuns` + a new additive `AllAgentRunListParams.WorkflowsOnly` filter (`WHERE d.source_template IS NOT NULL`), the shared `AgentRunRow`, batched report chips. Gallery + Runs now a two-tab shell (`workflowsTabBar`). Status filter = daisy `TabBar` box (All/Success/Error/In progress/Skipped, links preserve workflow filter); workflow filter = GET-form `<select>` of enabled workflows. Per-run **Re-run** via `OverflowMenu` → existing `POST /-/agents/{slug}/run` (no new REST surface → no OpenAPI drift). `AgentRunRow` gained an optional `Actions templ.Component` slot rendered OUTSIDE the nav anchor (own `list-row` grid column); nil everywhere else so `/agents` is byte-identical. `workflows_runs.js` retrigger factory; offset pagination + empty state. +1 integration test (`TestListAllAgentRuns_WorkflowsOnly`). 3 screenshots (runs / menu-open / gallery-tabs, img402). Full CI matrix green. **Deferred:** per-status run counts on the tabs (needs a GROUP BY query).

**Screenshot recipe (works in the autonomous loop):** build `bin/breadbox`; serve against the **dev `breadbox` DB** (apply this sprint's *additive* migrations to it — they're `ADD COLUMN … NULL`, safe; admin already exists) w/ `DATABASE_URL=…/breadbox ENCRYPTION_KEY=$(cat ~/.local/share/breadbox/dev-encryption-key) SERVER_PORT=8092 BREADBOX_DEV_RELOAD=1` (bg via `nohup …serve &` — the `nice` warning is harmless). Login via curl: GET `/login` for the `_csrf` hidden field, then POST `/login` with form fields **`username`** (=`admin@example.com`), **`password`** (=`password`), **`_csrf`** — NOT `email`/`csrf_token`; a 303 = success, then verify `/workflows` returns 200. Screenshot via Playwright + system Chrome: `chromium.launch({channel:'chrome',headless:true})`, read the HttpOnly `session` cookie from the curl jar (`grep $'\tsession\t'`, col 7), `ctx.addCookies([{name:'session',value,domain:'localhost',path:'/',httpOnly:true,sameSite:'Lax'}])`, goto, optionally click to open UI (e.g. `button:has-text("Set up")`), `page.screenshot({type:'jpeg',quality:88})`. **Gotchas:** (1) ESM ignores `NODE_PATH` — symlink the job-dir `node_modules` into a `$TMPDIR/wfshot/` workdir and run the script from there; the job dir itself is NOT sandbox-writable. (2) Chrome's profile-singleton socket bind fails under the sandbox — run the `node` command with `dangerouslyDisableSandbox:true` (img402 upload too). (3) Write `.mjs` with the Write tool, not a heredoc — zsh escapes `!` → `\!`. Upload `curl -X POST https://img402.dev/api/free -F image=@shot.jpg` → `.url`.
- **PR 6 — Governance basics (split):**
  - [x] **PR 6a — drawer governance (projected cost + consent).** ✅ MERGED #1630 (squash 83ea3fc1). `EstCostPerRunUSD` on the preset registry; drawer shows a projected-cost hint (reactive `≈$X/mo · N runs` by cadence for scheduled — `projectedCost()` JS; `≈$X/run` for post-sync). First-enable **consent ack**: required checkbox gating the Enable button (Alpine `:disabled`), stored household-global in `app_config` (`workflows.consent_acknowledged_at`), enforced server-side (`400 CONSENT_REQUIRED`) + recorded on first enable. New `Service.WorkflowsConsentAcknowledged`/`AcknowledgeWorkflowsConsent` + appconfig key. +2 integration tests. Full CI matrix green. 3 screenshots.
  - [x] **PR 6b-i — household spend ceiling.** ✅ MERGED #1632 (squash ea574959). `Orchestrator.checkHouseholdCeiling` gates `RunNowWith`/`RunNowAsyncWith`/`RunOrSkip` BEFORE `sem.Acquire`: when `KeyAgentGlobalMaxBudgetUSD` (>0) and rolling-30-day spend reaches it → cron leaves a `skipped` row (reason = ceiling msg), manual returns `ErrBudgetCeilingReached` (admin+REST handlers → **429 BUDGET_CEILING_REACHED**). Unset/≤0 = no cap; cost-sum query error **fails open** (logged). New sqlc `GetHouseholdCostSince` + `Service.HouseholdCostSince`/`HouseholdSpendStatus`. Repurposed the formerly-unenforced global-budget config as the household ceiling; re-captioned the settings field + added a "Spent (last 30 days) $X of $Y" row. +2 integration tests. Full CI matrix green. 1 screenshot.
  - **PR 6b-ii — remaining structural controls (split further):**
    - [x] **post-sync debounce.** ✅ MERGED #1633 (squash 69f76c14). `FireSyncCompleteAgents` skips a def that ran (non-skipped) within `PostSyncDebounceWindow` (15m, hardcoded) → N rapid syncs coalesce to 1 run (row-less skip, fails open). New sqlc `ExistsRecentRunForDefinition` + `Service.RecentRunExistsForDefinition`. +1 integration test; existing webhook-dispatch tests unregressed. Full CI matrix green. Backend-only.
    - [x] **admin-only enable** (RBAC). ✅ MERGED #1635 (squash c714c2d3). `RequireAdmin` upgrades `/-/workflow-presets/{slug}/enable` (editors → 403); gallery passes `IsAdmin` → non-admins see "Set up" disabled w/ lock + tooltip, drawers not rendered. **Scope:** gated *instantiation* only; the run toggle (resume/pause of an existing workflow) stays editor — deliberate narrow line. 2 screenshots (editor locked / admin normal).
    - [ ] **dependent-exclusion** honored in report presets (`is_dependent_linked = FALSE` in the preset prompts).
    - [x] near/over-ceiling **banner on the gallery.** ✅ MERGED #1634 (squash 3b75aa33). Gallery handler loads `HouseholdSpendStatus` → `WorkflowSpendBanner`: red "Workflows paused — spend ceiling reached" (spent ≥ ceiling) / amber "Approaching" (≥80%) / none. Pure surfacing of the #1632 ceiling; 2 screenshots. Full CI matrix green.

## After iteration 1 — keep improving (autonomous)
Refer to Mintlify (app.mintlify.com/breadbox/breadbox/products/workflows) for what production-ready looks like: every preset codified AND configurable, runs tab w/ status filters + retrigger + summaries, "Not set up" states, per-preset specialized options. Identify + fix gaps, add presets from the 13-preset backlog, build the notification sink (unblocks Sync Watchdog + Alerts), then the custom-workflow builder. Each as its own PR with evidence + notification. Keep going until Ricardo returns.

## Log (most recent first)
- **admin-only enable merged (#1635, c714c2d3)** — instantiating a workflow from a preset is now admin-only (`RequireAdmin` on the enable route; gallery "Set up" disabled+locked for non-admins, drawers withheld). Closes the §4.3 authorization gap. Completes the governance arc (consent → ceiling → debounce → banner → access control). 2 screenshots. Full CI green. **PR6b-ii remaining: dependent-exclusion in report presets only.** Bigger remaining: PR4b (rename — needs IA decision on hand-authored agents), PR4c (deferred DB rename). Polish backlog: per-status run counts on Runs tabs, 13-preset backlog, notification sink, custom-workflow builder.
- **gallery ceiling banner merged (#1634, 3b75aa33)** — surfaces the #1632 spend-ceiling state on /workflows: red "paused" (over) / amber "approaching" (≥80%) alert, reusing `HouseholdSpendStatus`. 2 screenshots. Full CI green. **PR6b-ii remaining: admin-only enable RBAC, dependent-exclusion in report presets.** Bigger remaining: PR4b (rename — needs an IA decision on hand-authored agents), PR4c (deferred breaking DB rename). Polish backlog: per-status run counts on Runs tabs, 13-preset backlog, notification sink, custom-workflow builder.
- **post-sync debounce merged (#1633, 69f76c14)** — `FireSyncCompleteAgents` coalesces rapid syncs: a def that ran within 15m is skipped row-less (fails open). New `ExistsRecentRunForDefinition` query. Pairs with the ceiling (#1632) to close the §4 cost-amplification trap. Full CI green. **Remaining: PR4b (rename agents→workflows API/UI/MCP), PR4c (breaking DB rename — needs shared-DB clear), PR6b-ii rest (admin-only enable RBAC, dependent-exclusion, gallery ceiling banner).** Then polish: per-status run counts on Runs tabs, 13-preset backlog, notification sink, custom-workflow builder.
- **PR 6b-i merged (#1632, ea574959)** — household spend ceiling: orchestrator gate (cron→skipped row / manual→429 BUDGET_CEILING_REACHED) on rolling-30-day spend vs `KeyAgentGlobalMaxBudgetUSD`; new `GetHouseholdCostSince` query + spend display in settings; fails open on query error. The previously-unenforced global-budget config is now live. Full CI matrix green. 1 screenshot. **Remaining: PR4b (rename agents→workflows API/UI/MCP), PR4c (breaking DB rename — needs shared-DB clear), PR6b-ii (debounce + admin-only RBAC + dependent-exclusion + gallery ceiling banner).** Then polish: per-status run counts on Runs tabs, 13-preset backlog, notification sink, custom-workflow builder.
- **PR 6a merged (#1630, 83ea3fc1)** — drawer governance: projected-cost hint (reactive ≈$/mo by cadence; ≈$/run post-sync) + first-enable consent ack (gated Enable button, app_config-stored, server-enforced `CONSENT_REQUIRED`). Full CI matrix green. 3 screenshots. **Remaining toward Mintlify bar: PR4b (rename agents→workflows API/UI/MCP), PR4c (breaking DB rename — needs shared-DB clear), PR6b (cost ceiling + debounce + admin-only RBAC + dependent-exclusion).** Then: per-status run counts on the Runs tabs, 13-preset backlog, notification sink, custom-workflow builder.
- **PR 5c merged (#1628, 2e9c7ca1)** — Workflows Runs tab. `/workflows/runs` (WorkflowsOnly filter), status TabBar + workflow filter, per-run Re-run via OverflowMenu (reuses `/-/agents/{slug}/run`), `AgentRunRow.Actions` slot (nil → /agents unchanged), Gallery/Runs two-tab shell. Full CI matrix green. 3 screenshots. **Iteration-1 PR checklist now: PR1–5 all merged; remaining = PR4b (rename API/UI/MCP), PR4c (breaking DB rename — needs shared-DB clear), PR6 (governance).** Next-highest-value after those: per-status run counts on the tabs, the 13-preset backlog, notification sink, custom-workflow builder.
- **PR 5b merged (#1626, c1d9d483)** — configure drawer. "Set up" → right slide-over (schedule select / post-sync line, additional-instructions textarea, start-now checkbox). Service params + handler + 2 integration tests. All CI green (default/headless/lite). 2 screenshots. **Next: PR 5c (runs tab re-skin)** — then PR4b rename, PR6 governance, 13-preset backlog.
- (init) Sprint branch created off main c285101b; `workflows/**` added to CI triggers; sprint-state seeded.
